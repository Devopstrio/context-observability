# Getting Started

```bash
git clone https://github.com/enterprise-context-engineering/context-observability.git
cd context-observability
python -m venv .venv
source .venv/bin/activate
make install-dev
make docker-up
```
